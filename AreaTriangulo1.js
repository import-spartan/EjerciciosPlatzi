let base = 5 
let altura = 7

function triangleArea(base, altura) {
	return base * altura / 2
}

console.log(`El área de un triángulo de base
 ${base} y altura ${altura} es: ${triangleArea(base, altura)}`)